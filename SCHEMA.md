# סכימת קובץ נכס

כל נכס הוא קובץ אחד: `properties/<id>.json`.
הבנייה נכשלת אם משהו חסר או לא תקין, ואומרת בדיוק איזה שדה ובאיזה קובץ.

```bash
py build.py --check    # בדיקה בלבד
py build.py            # בדיקה + בנייה
```

## שדות חובה

| שדה | סוג | הערות |
|---|---|---|
| `id` | טקסט | אותיות אנגליות קטנות, ספרות ומקפים. **חייב להיות זהה לשם הקובץ**. אצל לקוח אמיתי — ראה אזהרה למטה |
| `name.he` | טקסט | שם הנכס בעברית |
| `wifi.ssid` | טקסט | שם הרשת |
| `wifi.pass` | טקסט | הסיסמה |
| `checkin.from` | טקסט | שעת כניסה, למשל `"15:00"` |
| `checkin.to` | טקסט | שעת יציאה |
| `host.whatsapp` | טקסט | **ספרות בלבד**, כולל קידומת מדינה. `972501234567` |

## ⚠️ המזהה של לקוח אמיתי חייב להיות בלתי ניתן לניחוש

סיסמת ה‑Wi‑Fi יושבת בעמוד ציבורי. הוא לא מופיע בגוגל, אבל כתובת כמו `/g/red-view/` קלה מדי לניחוש.

**לכל נכס אמיתי — סיומת אקראית:**

```bash
py -c "import secrets; print(secrets.token_hex(3))"
```

```
red-view-7fk3n2    ✓ ללקוח אמיתי
red-view           ✗ רק לדוגמאות
```

## שדות רשות

**כלל אחד חשוב: שדה שלא קיים — המקטע שלו פשוט לא מוצג.** אין מקטע ריק ואין שגיאה.

| שדה | סוג | מה זה |
|---|---|---|
| `demo` | true/false | מציג פס צהוב "נכס לדוגמה" |
| `name.en` | טקסט | שם באנגלית |
| `area` | `{he,en}` | אזור כללי, לא כתובת |
| `checkin.entry` | `{he,en}` | הוראות כניסה |
| `parking.text` | `{he,en}` | מידע חניה |
| `parking.alt` | `{he,en}` | מה עושים אם אין מקום |
| `parking.nav` | כתובת | קישור ניווט. חייב להתחיל ב‑`https://` |
| `host.label` | `{he,en}` | שם בעל הנכס |
| `rules` | רשימת `{he,en}` | כללי הבית |
| `devices` | רשימת `{name:{he,en}, how:{he,en}}` | הוראות הפעלה |
| `emergency` | רשימת `{name:{he,en}, tel}` | `tel` חובה בכל פריט |
| `tips` | רשימת `{name:{he,en}, note:{he,en}}` | המלצות |
| `review.url` | כתובת | חייב להתחיל ב‑`https://` |

## שתי שפות

כל שדה טקסט הוא אובייקט `{ "he": "...", "en": "..." }`.

**אם `en` חסר — מוצג הטקסט העברי.** לא ריק, לא שגיאה. אפשר להתחיל לקוח בעברית בלבד ולהוסיף אנגלית אחר כך.

## דוגמה מינימלית

```json
{
  "id": "my-flat",
  "name": { "he": "הדירה שלי" },
  "wifi": { "ssid": "MyFlat", "pass": "1234abcd" },
  "checkin": { "from": "15:00", "to": "11:00" },
  "host": { "whatsapp": "972501234567" }
}
```

זה עובד. מציג Wi‑Fi, שעות וכפתור WhatsApp. כל השאר לא יופיע.

## לקוח חדש — שלושה צעדים

```bash
# 1. צור properties/<id>.json לפי הסכימה
# 2. בנה
py build.py
# 3. צור QR לכל הנכסים
py make-qr.py --all https://orr19988-dotcom.github.io/kod-habait
```

ואז `git add . && git commit && git push`.

## מה לא לערוך ידנית

התיקיות `g/` ו‑`a/` **נכתבות מחדש בכל בנייה.** כל שינוי ידני שם יימחק.
רוצה לשנות תוכן — ערוך את קובץ הנכס. רוצה לשנות עיצוב — ערוך את `template.html`.
